{
  "name": "multivendor-application-observability",
  "version": "0.0.6-beta",
  "description": "Enterprise Observability Abstraction (OTel, NR, Datadog)",
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
